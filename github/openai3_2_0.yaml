repo:
  name: compiler
  owner: dingemoe
  url: https://github.com/dingemoe/compiler.git
  access: public
  permissions:
    read: true
    write: true
    actions: [edit, read, commit, push]
openai:
  version: 3.2.0
  usage: unrestricted
  description: "Åpent prosjekt for testing og utvikling uten begrensninger."
  contributors:
    - dingemoe
    - brukere
gpt_actions:
  enabled: true
  allowed_operations:
    - read_repository
    - edit_repository
    - commit_changes
    - push_changes
